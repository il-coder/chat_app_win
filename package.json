{
  "name": "chat_app",
  "productName": "Chat App",
  "version": "1.0.2",
  "description": "WEBSOCKET BASED CHAT APPLICATION",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . chat_app --overwrite --asar=true --platform=win32 --arch=ia32 --icon=ico.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Chat App\""
  },
  "author": "PIYUSH GARG",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.0.2",
    "electron-packager": "^14.2.1"
  }
}
